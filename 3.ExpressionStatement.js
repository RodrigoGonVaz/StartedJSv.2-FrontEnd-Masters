/*
• Expressions and Statements 
*/

let age = 39; // Statement - (assigmente expression age = 39)

age = 1 + (age * 2); // Expressions - (2=literal expression) - (age=expression) - (age*2= expression), etc... (6 expressions)


/*
• Decisions
*/

let age = 39; 
// It will make a decision if it get a true value
if (age >= 18) {
    goVote();
}

// It will make a decision if it get a true value and if it get a false value it will do de else
let isEnrolled = true;

if (isEnrolled) {
    console.log('take th Class')
} else {
    console.log('Enroll First')
}